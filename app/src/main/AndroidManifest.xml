<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App_tim_viec">
        <activity android:name=".UI.hosocanhan.ActivityHoSoNTV"
            tools:ignore="MissingClass" />
        <activity android:name=".UI.hosocanhan.ActivityChinhSuaHoSo"/>
        <activity android:name=".UI.Nguoi_Tim_Viec.hosocanhan.ActivityEditProfile" />
        <activity android:name=".UI.Nguoi_Tim_Viec.hosocanhan.ActivityeditInfcontact" />
        <activity android:name=".UI.Nguoi_Tim_Viec.tinnhan.ActivityTinNhan" />

        <!-- Activity chứa Fragment Đăng nhập -->
        <activity
            android:name=".UI.Xac_Thuc.dangnhap.ManHinhDangNhap"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity chính sau đăng nhập -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />



    </application>
</manifest>